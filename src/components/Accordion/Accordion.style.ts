import styled from "styled-components";

const MainContainer = styled.div`
  display: flex;
  flex-direction: row;
  gap: 30px;
  justify-content: start;
  align-items: center;
  width: 1200px;
  height: 480px;
  margin: 0 auto;
`;

export default {
  MainContainer,
}
